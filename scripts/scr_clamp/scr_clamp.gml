/// @description scr_clamp(value, min, max) */
/// @param value
/// @param  min
/// @param  max
function scr_clamp(argument0, argument1, argument2) {
	// Keeps value within the range of min and max.

	    return min(max(argument0, argument1), argument2);



}
